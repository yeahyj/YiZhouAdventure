/**地图层级 */
export enum MapLayersType {
    MAP,
    ENVIRONMENT,
    ENTITY,
    SKILL,
    VFX,
    TIP,
    TOP,
    NUM,
}

/**地图层级名字 */
export const MapLayerNames = ['map', 'environment', 'entity', 'skill', 'vfx', 'tip', 'top'];
